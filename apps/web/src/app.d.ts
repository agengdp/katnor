// See https://svelte.dev/docs/kit/types#app.d.ts for information about
// these interfaces. Left as placeholders in Phase 0 - Locals/PageData will
// gain shape once auth (owner session) and page `load` functions exist.
declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }
}

export {};
