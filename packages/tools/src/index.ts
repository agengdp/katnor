// @katnor/tools - the tool registry, MCP client, sandbox tools (shell, git,
// github, figma, browser), and coding-agent tool wrappers (claude_code,
// codex). See PLAN.md section 4.3.
//
// TODO: implemented in a later phase

export const TOOLS_PACKAGE_NAME = '@katnor/tools';

export type ToolFamily = 'company' | 'work' | 'mcp';
